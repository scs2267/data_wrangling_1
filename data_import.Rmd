---
title: "Simple document"
output: html_document
---

```{r}
library(tidyverse)
```

##Read in some data

Read in litters data set.

```{r}
litters_df = read.csv("./data_import_examples/FAS_litters.csv")
litters_df = janitor::clean_names(litters_df) ##loads the janitor package without using library function - this clean names function just cleans up the names
```

##Take a look at the data



